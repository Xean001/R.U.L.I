using UnityEngine;

/// <summary>
/// Marca un objeto como liana escalable. Necesita un Collider2D en modo trigger.
/// Ruli puede agarrarse (tecla W/Arriba), subir/bajar y saltar con Espacio a la
/// siguiente liana.
/// </summary>
[RequireComponent(typeof(Collider2D))]
public class Liana : MonoBehaviour
{
}
